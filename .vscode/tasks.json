{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build",
            "type": "shell",
            "command": "/opt/gowin/IDE/bin/gw_sh ${workspaceFolder}/scripts/build.tcl",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "options": {
                "cwd": "${workspaceFolder}"
            },            
        }
    ]    
}